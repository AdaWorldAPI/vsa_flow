[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
startCommand = "uvicorn vsa_flow.api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
